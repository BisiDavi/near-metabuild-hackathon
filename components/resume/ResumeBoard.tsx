import ResumeBoardProvider from '@/providers/ResumeBoardProvider';

export default function ResumeBoard() {
  return (
    <ResumeBoardProvider>
      <section>ResumeBoard</section>
    </ResumeBoardProvider>
  );
}
